# Local tooling state
.context/
.DS_Store
node_modules/

# experiment run artifacts (archived sessions in runs-archive/)
runs/
runs-archive/

# generated by golden/judge-validation.mjs cold-start harness
golden/judge-validation-fixture/cold-start-run/

# Scratch probes a run writes next to the pipeline scripts while diagnosing a
# live app (scripts/_tmp-probe*.mjs, _tmp-shot.mjs). Run artifacts, not source.
scripts/_tmp-*.mjs
